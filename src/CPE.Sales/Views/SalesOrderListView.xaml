<local:ViewBase x:Class="CPE.Sales.Views.SalesOrdersListView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:local="clr-namespace:CPE.Sales.Views"
                xmlns:dataTemplates="clr-namespace:CPE.Sales.DataTemplates"
                Loaded="SalesOrderMailListView_OnLoaded"
                Width="265"
                DataContext="{Binding MailListViewModel, Source={StaticResource BootstrapLocator}}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<TextBlock x:Name="HeaderLabel" Text="Retrieving sales orders..." FontSize="14" FontWeight="Bold" Margin="0,3" />
		<ListBox Grid.Row="1" ItemsSource="{Binding SalesOrders}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<dataTemplates:SalesOrderListDataTemplate />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</local:ViewBase>